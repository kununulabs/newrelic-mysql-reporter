metrics:
  - name: "ExampleMetric"
    comment: "This is just an example metric, reporting that the database is accessible."
    query: "SELECT 1;"
  - name: "AnotherExampleMetric"
    comment: "This is just another example metric, reporting that the database is accessible."
    query: "SELECT 2;"
attributes:
  application: example
  deployment: development
